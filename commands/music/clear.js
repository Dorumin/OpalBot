/* Anyone can clear, anyone can undo. Mods can clear and confirm */
module.exports = (OpalBot) => {
    const out = {},
    i18n = OpalBot.i18n;

    out.peasants = {};
    out.peasants.clear = (message, content, lang) => {

    };

    return out;
}